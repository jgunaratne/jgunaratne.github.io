{
  "name": "juniusg-site",
  "private": true,
  "description": "Dev tooling only. The site itself ships as static files with no build step.",
  "scripts": {
    "serve": "python3 scripts/serve.py 8000",
    "test": "playwright test",
    "format": "prettier --write .",
    "check": "prettier --check . && python3 scripts/check-links.py"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "prettier": "^3.4.0"
  }
}
